<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin" >

    <ListView
        android:id="@+id/volunteer_list"
        android:layout_width="match_parent"
        android:layout_height="0dp" 
        android:layout_weight="@string/map_weight"/>
    
    <Button
            android:id="@+id/set_new_location"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_gravity="bottom"
            android:background="@color/DarkCyan"
            android:text="@string/button_change_location"
            android:textColor="@color/White"
            android:layout_weight="1"
            android:textSize="@dimen/button_change_location_textSize" 
            android:onClick="onSetNewLocation"/>

</LinearLayout>